# KSitePulse

`ksitepulse` is a Chrome extension that monitors page health in real time and reports it to users in two modes: a minimal Everyday Mode (plain-language summary, action-oriented) and a detailed Developer Mode (metrics dashboard, waterfall, console stream). It operates passively — no user action required — and surfaces feedback only when meaningful.